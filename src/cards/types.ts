export interface Theme {
  backgroundColor: string;
  borderColor: string;
  textColor: string;
  titleColor: string;
  badgeColor: string;
}

export interface Badge {
  logoName: string;
  label: string;
  logoColor: string;
}
